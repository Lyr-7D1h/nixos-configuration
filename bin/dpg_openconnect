#!/bin/bash

set -e

# Did not work

# BW_SESSION=`bw unlock --raw`
# PASSWORD=`bw get password onelogin --session $BW_SESSION`

sudo openconnect --juniper https://homeworker.persgroep.net -u ivo.velthoven@persgroep.net # -p $PASSWORD
